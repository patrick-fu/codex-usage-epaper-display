# Domain Docs

This is a single-context repository.

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. Missing files are not errors; proceed silently.

Use terminology defined in `CONTEXT.md`. If work conflicts with an ADR, surface the conflict instead of silently overriding it.

Layout:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
